{
  "name": "fastview-sdci",
  "version": "1.0.0",
  "description": "The **Seattle Services Portal** (`services.seattle.gov/Portal`) is operated by the Seattle Department of Construction & Inspections (SDCI). It tracks permits for construction, land use, and trade work across the city.",
  "main": "index.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/main.js",
    "main": "npm run -s build && npm run -s start",
    "start:server": "node dist/server/index.js",
    "server": "npm run -s build && node dist/server/index.js",
    "typecheck": "tsc -p tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "playwright": "^1.59.1",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "node-html-parser": "^7.1.0"
  }
}
